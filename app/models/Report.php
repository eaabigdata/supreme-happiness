<?php

class Report extends \Eloquent {
    protected $table = "reports";

    protected $rules = [
        // set up rules for each field
    ];

	protected $fillable = [
        // define fillable data
    ];
}